server:
  port: 8085

spring:
  application:
    name: loja-backend
  datasource:
    url: jdbc:postgresql://localhost:5437/loja_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
